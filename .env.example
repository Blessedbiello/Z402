# Root environment variables (if needed)
# Most variables should be in packages/backend/.env.example and packages/frontend/.env.example

# Docker Compose environment variables
COMPOSE_PROJECT_NAME=z402
